# Jingle assets

Place cleared `.mp3` or `.ogg` station IDs here, then add the filename to `public/audio/catalog.json` under `jingles`. The first listed file plays between segments. If unavailable, a short three-note Web Audio ident is generated.
